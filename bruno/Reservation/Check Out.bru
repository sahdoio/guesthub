meta {
  name: Check Out
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/reservations/{{reservationId}}/check-out
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("should return 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("should check out guest", function() {
    expect(res.getBody().message).to.equal('Guest checked out.');
  });
}
